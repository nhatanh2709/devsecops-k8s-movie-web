apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bluegreen-demo-preview
  namespace: test-rollouts
spec:
  ingressClassName: nginx
  rules:
  - host: argo-green.nhatanhmovieweb.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bluegreen-demo-preview
            port:
              name: http