apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kong-admin-ingress
  namespace: kong
spec:
  ingressClassName: nginx
  rules:
    - host: kong-admin.nhatanhmovie.website
      http: 
        paths: 
          - backend: 
              service:
                name: kong-gateway-kong-admin
                port: 
                  number: 8001
            path: /
            pathType: Prefix
