namespace: kong

ingressController:
  enabled: true

proxy:
  type: ClusterIP
  http:
    enabled: true
    servicePort: 8000
  tls:
    enabled: false

admin:
  enabled: true
  type: ClusterIP
  http:
    enabled: true
    servicePort: 8001
    containerPort: 8001
  ingress:
    enabled: false
  tls: 
    enabled: true

manager:
  enabled: true
  type: ClusterIP
  http:
    enabled: true
    servicePort: 8002
    containerPort: 8002
  tls:
    enabled: true
  ingress:
    enabled: false

env:
  database: postgres
  pg_database: kong
  pg_user: kong
  pg_password: nhatanh2709
  pg_host: postgres.kong.svc.cluster.local
  admin_gui_url: "https://kong-manager.nhatanhmovie.website"
  admin_gui_api_url: "https://kong-admin.nhatanhmovie.website"