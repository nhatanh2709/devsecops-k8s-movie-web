apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-pvc
  namespace: kong
spec:
  accessModes:
  - ReadWriteOnce
  resources: 
    requests:
      storage: 5Gi
  storageClassName: nfs-storage