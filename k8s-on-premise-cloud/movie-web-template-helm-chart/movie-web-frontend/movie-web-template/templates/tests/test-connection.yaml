apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movie-web-template.fullname" . }}-test-connection"
  labels:
    {{- include "movie-web-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movie-web-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
